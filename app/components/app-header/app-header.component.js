angular
    .module('choosing-app')
    .component('appHeader', {
        templateUrl: "components/app-header/app-header.template.html",
        controller: ($scope) => {
            $scope.handelLogout = () => {
                
            }
        }
    });